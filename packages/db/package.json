{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./build/db.js",
  "types": "./build/db.d.ts",
  "exports": {
    ".": {
      "types": "./build/db.d.ts",
      "default": "./build/db.js"
    }
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc -b",
    "pretest": "tsc -b test && node test/build/setup_db.js",
    "test": "node --test --test-concurrency=1 'test/build/**/*.test.js'"
  },
  "dependencies": {
    "@types/node": "^25.5.0",
    "@types/pg": "^8.20.0",
    "pg": "^8.20.0",
    "typescript": "^5.9.3"
  }
}
