---
id: malformed-node
title: A node whose metadata block will not parse
this line is not a key
type: concept
status: current
audiences: [researcher]
---

# A node whose metadata block will not parse

Exists so the loader can be shown forwarding a parse failure as a warning
against one document, rather than letting it end the corpus.
