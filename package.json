{
  "name": "web-chatbot-prototype",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:chatbot": "cd packages/aim_hi_chatbot && npm run build",
    "build:webserver": "cd packages/aim_hi_webserver && npm run build",
    "build:fe": "cd packages/aim_hi_fe && npm run build"
  },
  "devDependencies": {
    "fastify": "^5.8.4",
    "typescript": "^5.8.0"
  }
}
